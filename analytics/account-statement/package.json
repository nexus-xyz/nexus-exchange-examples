{
  "name": "account-statement",
  "version": "0.0.0",
  "private": true,
  "description": "A period P&L, fee and funding statement for one Nexus Exchange account, reconciled against the venue's own equity curve.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/*.test.ts",
    "build": "npm run test --silent"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "tsx": "4.23.8",
    "typescript": "7.0.2"
  }
}
