{
  "name": "bracket-orders",
  "version": "0.0.0",
  "private": true,
  "description": "A bracket order manager for Nexus Exchange: an entry plus reduce-only take-profit and stop-loss exits, one-cancels-other client-side, on the TypeScript SDK.",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/bracket.test.ts src/run.test.ts",
    "build": "npm run test --silent"
  },
  "dependencies": {
    "@nexus-xyz/exchange-ts": "0.5.0"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "tsx": "4.23.8",
    "typescript": "7.0.2"
  }
}
